services:
  - type: web
    name: lsports-inplay
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lsports-db
          property: connectionString

databases:
  - name: lsports-db
    plan: free
